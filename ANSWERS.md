What are PropTypes used for?
- PropTypes are used to validate the type of a prop. So if a prop is suppose to be an array, we can use PropTypes to check for this. 

What is a lifecycle event in React?
- Not sure what a lifecycle event is, but if there's any relation to lifecycle methods, than these methods allow us to tap into the lifecycle (mounting/unmounting/getting props) of a component with reserved methods.

What is a Higher Order Component?
-A Higher Order component is basically a component that returns another component. 

What are three different ways to style components in React?
- Using the CSS-in-JS with a library called styled-components
- Using plain old CSS (inline or in a separate file)
- Using a Pre-processor (which I don't know how to do but guessing it's possible :)