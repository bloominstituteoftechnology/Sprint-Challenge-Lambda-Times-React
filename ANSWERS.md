<!--    1. PropTypes are used to check the type of the props being passed to a component.
        2. A lifecycle event in React is componentDidMount().
        3. A higher order component is a component that accepts a component argument and returns a component.
        4. You can style a component in React using standard CSS styling, in-line styling, or styled-components.
-->